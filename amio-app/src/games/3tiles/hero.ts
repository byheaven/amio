import { HeroConfig } from '@/engine/types';

export const threeTilesHeroConfig: HeroConfig = {
  enabled: true,
  mode: 'hero',
  description: 'Hard mode with denser stacks and one chance to maximize chest upgrade.',
};
